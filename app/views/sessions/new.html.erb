<% provide(:title, t( "login.attr.title" )) %>
<h1><%= t "login.attr.title" %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(:session, url: login_path) do |f| %>
      <%= f.label :email %>
      <%= f.email_field :email, class: "form-control" %>
      <%= f.label :password %>
      <%= f.password_field :password, class: "form-control" %>
      <%= f.submit t( "login.attr.title" ), class: "btn btn-primary" %>
    <% end %>
    <p><%= t "login.attr.new_user" %>
      <%= link_to t( "home.signup" ), signup_path %></p>
  </div>
</div>
